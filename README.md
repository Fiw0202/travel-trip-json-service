# Travel trip json-server

To run the mock service, you need to set up a JSON server
- Install JSON server globally (if not already installed):
```js
npm install -g json-server
```

- Start JSON server:
```js
json-server --watch server.json
```
